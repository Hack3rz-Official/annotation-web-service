# marks this directory as the root lombok directory
config.stopBubbling = true
# adds an annotation that will prevent jacoco (code coverage tool) from counting these lines
lombok.addLombokGeneratedAnnotation = true